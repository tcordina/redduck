{{ form_start(form, {'attr': {'id': 'form-comment-post'} }) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">{{ button_label|default('Commenter') }}</button>
{{ form_end(form) }}
